# Question 1: File Creation and Deletion
# 1. Create a file named "example.txt" using the "x" mode and write some content in it.
#    - Check if "example.txt" exists, and if so, delete it.
#    - If it does not exist, print a message saying so.